[server]
headless = true
address = "0.0.0.0"
port = 8501

[browser]
gatherUsageStats = false

[client]
# Hide the "Deploy" button + developer menu for a clean, product-like UI.
toolbarMode = "minimal"

[theme]
base = "light"
primaryColor = "#2563eb"
